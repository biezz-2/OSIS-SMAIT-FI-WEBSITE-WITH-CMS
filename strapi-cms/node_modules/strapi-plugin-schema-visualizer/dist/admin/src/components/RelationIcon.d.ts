import React from "react";
import "./RelationIcon.css";
interface RelationIconProps {
    theme: {
        colors: {
            neutral0: string;
            buttonPrimary500: string;
            [key: string]: string;
        };
    };
    children: React.ReactNode;
}
export declare function RelationIcon({ theme, children }: RelationIconProps): React.ReactElement;
export {};
