import { NodeChange, EdgeChange, Connection, Edge as ReactFlowEdge, Node as ReactFlowNode } from "reactflow";
import { ContentType, Options } from "../utils/dataUtils";
interface DiagramState {
    nodes: ReactFlowNode<any>[];
    edges: ReactFlowEdge<any>[];
    data: ContentType[];
    showModal: boolean;
    options: Options & {
        snapToGrid: boolean;
        showTypes: boolean;
        showIcons: boolean;
        showRelationsOnly: boolean;
        showAdminTypes: boolean;
        showDefaultFields: boolean;
        showPluginTypes: boolean;
        scrollMode: boolean;
        backgroundPattern: string;
        layoutDirection: string;
    };
    setData: (contentTypesData: ContentType[]) => void;
    setShowModal: (bool: boolean) => void;
    toggleOption: (optionName: string, optionValue?: any) => void;
    onNodesChange: (changes: NodeChange[]) => void;
    onEdgesChange: (changes: EdgeChange[]) => void;
    onConnect: (connection: Connection) => void;
    drawDiagram: () => void;
    redrawNodes: () => void;
    redrawEdges: () => void;
    applyLayout: () => void;
}
export declare const useDigramStore: import("zustand").UseBoundStore<import("zustand").StoreApi<DiagramState>>;
export {};
