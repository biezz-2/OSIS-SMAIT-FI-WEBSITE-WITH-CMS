'use strict';

var adminViteSingletonModules = require('./admin-vite-singleton-modules.js');

/**
 * Modules given explicit Vite resolve aliases (and included in resolve.dedupe) for the admin bundle.
 * Single source of truth for resolution contract tests.
 *
 * @internal
 */ const ADMIN_VITE_ALIAS_MODULES = [
    'react',
    'react-dom',
    'react-router-dom',
    'styled-components',
    'react-redux',
    '@reduxjs/toolkit',
    '@strapi/design-system',
    '@radix-ui/react-tooltip',
    'lodash',
    'invariant',
    'prismjs'
];
/**
 * Modules passed to Vite resolve.dedupe (and aliased): the admin alias modules plus the
 * CodeMirror singletons, so every copy collapses onto a single runtime instance.
 */ const ADMIN_VITE_DEDUPE_MODULES = [
    ...ADMIN_VITE_ALIAS_MODULES,
    ...adminViteSingletonModules.ADMIN_VITE_SINGLETON_MODULES
];

exports.ADMIN_VITE_SINGLETON_MODULES = adminViteSingletonModules.ADMIN_VITE_SINGLETON_MODULES;
exports.ADMIN_VITE_ALIAS_MODULES = ADMIN_VITE_ALIAS_MODULES;
exports.ADMIN_VITE_DEDUPE_MODULES = ADMIN_VITE_DEDUPE_MODULES;
//# sourceMappingURL=admin-vite-alias-modules.js.map
